// call this inside handleTileChange

// checkIfPlacementRestricted
// restrictedPosition
export default function restrictPlacement(setRestrictedPosition: number) {
    return  0
}

// useState of 

// if couitner = 2
// setrescitre either x or y to the tile x or tile y